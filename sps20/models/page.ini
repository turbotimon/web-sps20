[model]
name = Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.menu_title]
label = Menu Title
type = string
description = If this is set, it will override the title in a menu (e.g. the main navigation).

[fields.body]
label = Body
type = markdown
